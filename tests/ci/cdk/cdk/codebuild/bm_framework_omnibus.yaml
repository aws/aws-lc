# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

version: 0.2

# Doc for batch https://docs.aws.amazon.com/codebuild/latest/userguide/batch-build-buildspec.html#build-spec.batch.build-list
batch:
  build-list:

    # TODO(CryptoAlg-1283): Remove below when benchmark CI supports FIPS branches.
    # A dummy dimension is used here because the batch CodeBuild does not allow empty |build-list|.
    - identifier: batch_does_not_allow_empty_list
      buildspec: ./tests/ci/codebuild/batch_does_not_allow_empty.yml
      env:
        type: LINUX_CONTAINER
        privileged-mode: true
        compute-type: BUILD_GENERAL1_SMALL
        image: aws/codebuild/standard:4.0

    # TODO(CryptoAlg-1283): Enable below when benchmark CI supports FIPS branches.
    # - identifier: ubuntu2004_bm_framework
    #   buildspec: ./tests/ci/codebuild/dummy.yml
    #   env:
    #     type: LINUX_CONTAINER
    #     privileged-mode: true
    #     compute-type: BUILD_GENERAL1_LARGE
    #     image: 620771051181.dkr.ecr.us-west-2.amazonaws.com/aws-lc-docker-images-linux-x86:ubuntu-20.04_clang-7x-bm-framework_latest
