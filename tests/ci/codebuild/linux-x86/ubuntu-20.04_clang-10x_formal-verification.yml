# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

version: 0.2

phases:
  pre_build:
    commands:
      - export CC=clang-10
      - export CXX=clang++-10
  build:
    commands:
      # CODEBUILD_SOURCE_REPO_URL and CODEBUILD_SOURCE_VERSION are defined by AWS CodeBuild. See below link
      # https://docs.aws.amazon.com/codebuild/latest/userguide/build-env-ref-env-vars.html
      - ./tests/ci/run_formal_verification.sh ${CODEBUILD_SOURCE_REPO_URL} ${CODEBUILD_SOURCE_VERSION}
