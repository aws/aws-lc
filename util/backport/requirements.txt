# Runtime dependencies for the AI layer (engine/ai.py), which always runs and needs
# AWS credentials. Everything else needs only the standard library + git.
#
# Without these installed, or without credentials, the tool still works and still
# misses nothing -- it warns loudly and flags every branch it could not judge for
# review, so you just get more flags than usual.
anthropic>=0.39
boto3>=1.34
